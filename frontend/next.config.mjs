/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['scontent.ftun14-1.fna.fbcdn.net'], // Add the domain without the protocol
    },
};

export default nextConfig;
